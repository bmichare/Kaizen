import React from 'react';
import { connect } from 'react-redux';

const mapStateToProps = (state) => ({
});

const AddLogDetails = (props) => {
  return (
    <div>
      from AddLogDetailsa
    </div>
  );
};

export default connect(mapStateToProps, null)(AddLogDetails);
